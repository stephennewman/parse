{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "includeFiles": [
          "node_modules/**" 
        ],
        "buildCommand": "apt-get update && apt-get install -y ffmpeg && npm run build"
      }
    }
  ]
} 