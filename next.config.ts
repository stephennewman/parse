import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Note: We might need to add experimental: { serverActions: true } here later
  // if we use Server Actions, but let's keep it minimal for now.
};

export default nextConfig;
